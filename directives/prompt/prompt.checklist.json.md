---
# Checklist (Strict JSON)

Return JSON only:
```json
{
  "title": "string",
  "master": ["string"],
  "specialized": [{"name":"string","steps":["string"]}],
  "stops":["string"],
  "failure_modes":["string"]
}
```

No extra keys.
